// CustomCursor is now handled entirely via CSS in index.css.
// The body cursor: url(SVG) is set globally.
// This component is kept as a no-op for hot-reload stability.
export default function CustomCursor() {
  return null;
}
